---
import Layout from '../layouts/Layout.astro';
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import { getCollection } from 'astro:content';
import {Content, getHeadings} from '../content/examenPracticasC3/intro.md';

const headings = getHeadings();

// Get all markdown files from the practicas collection
const practices = await getCollection('examenPracticasC3');

// Create the sidebar items
const sidebar = practices.map(practice => ({
    label: practice.data.title,  // Uses the title from frontmatter
    link: `./examenPracticasC3/${practice.slug}` // Creates the link using the file slug
}));

---

<Layout>
    <StarlightPage 
        frontmatter={{
            title: 'Enunciado C3 2025',
        }} 
        headings={headings}
        sidebar={sidebar}
    >
        <Content />
    </StarlightPage>
</Layout>